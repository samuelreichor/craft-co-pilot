description: "CoPilot System Prompt Eval Suite"

prompts:
  - id: file://generated/system-prompt.json
    label: "current"

providers:
  - id: openai:gpt-4o
    label: "gpt-4o"
    config:
      temperature: 0
      tools: file://generated/tools.json

tests:
  - file://tests/tool-selection.yaml
  - file://tests/safety.yaml
  - file://tests/field-formats.yaml
  - file://tests/brand-voice.yaml
