{% macro configWarning(setting) -%}
    {{ 'This is being overridden by the `{setting}` config setting in `config/co-pilot.php`.'|t('co-pilot', { setting: setting })|markdown(inlineOnly=true) }}
{%- endmacro %}
